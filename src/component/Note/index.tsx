import React from 'react';
import { Contain, Input, Button } from '../Note/style'


const Note: React.FC = () => {
    return (
        <Contain>
             <Input></Input>
             <Button>Add Note</Button>
            
        </Contain>
    )
}

export default Note 
